import "./introTexts.scss";
import React from "react";

const IntroTexts = () => {
    return (
        <div className="texts">
            <p>
                A pasonate dev who loves to create things that lives in the web
            </p>
        </div>
    );
};

export default IntroTexts;
